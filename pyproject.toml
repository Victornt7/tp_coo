[project]
name = "tp-coo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
[tool.ruff]
extend-exclude = ["*/migrations/*"]
